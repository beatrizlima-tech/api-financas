spring:
  application:
    name: api-financas

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false
  rabbitmq:
    listener:
      simple:
        auto-startup: false

server:
  port: 8083

cors:
  allowed-origins: http://localhost:4200

jwt:
  issuer: api-autenticacao
  secret: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=

integrations:
  agentes-ia:
    base-url: http://localhost:8084
    api-key: chave-interna-ficticia-para-testes

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

management:
  health:
    rabbit:
      enabled: false
